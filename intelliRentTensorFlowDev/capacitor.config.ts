import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.production.intelliRentDev',
  appName: 'intelliRentDev',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
